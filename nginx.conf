# Redirect HTTP → HTTPS
server {
    listen 80;
    server_name sermon.donbarger.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS — main config
server {
    listen 443 ssl;
    server_name sermon.donbarger.com;

    ssl_certificate     /etc/letsencrypt/live/sermon.donbarger.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sermon.donbarger.com/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Allow file uploads up to 20MB (sermon docs)
    client_max_body_size 20M;

    location / {
        proxy_pass         http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # SSE streaming — must disable buffering
        proxy_buffering        off;
        proxy_cache            off;
        proxy_read_timeout     120s;
        proxy_connect_timeout  10s;

        # Keep connections alive for streaming
        proxy_set_header Connection '';
        chunked_transfer_encoding on;
    }
}
